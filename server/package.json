{
  "name": "contractiq",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run dev --prefix Client\" \"cd server && python -m uvicorn main:app --reload\"",
    "install:all": "npm install --prefix Client && pip install -r server/requirement.txt --break-system-packages"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
